[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

[callback_default]
# Readable multi-line output. Replaces the removed community.general.yaml callback.
result_format = yaml
